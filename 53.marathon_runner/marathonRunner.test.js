const marathonRunner = require('./marathonRunner');

xdescribe(marathonRunner.name, () => {
  it('takes in file and returns the fastest marathon time out of the results.', () => {
    const result = marathonRunner();

    expect(result).toBe();
  });
});
